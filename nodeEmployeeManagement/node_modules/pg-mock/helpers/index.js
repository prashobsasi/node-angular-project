'use strict';

const sqlHelper = require('./sql');

function rnd(min, max) {
  var rand = min - 0.5 + Math.random() * (max - min + 1);
  rand = Math.round(rand);
  return rand;
}

module.exports = {
  rnd,
  parseSQL: sqlHelper.prepareSQL
};
