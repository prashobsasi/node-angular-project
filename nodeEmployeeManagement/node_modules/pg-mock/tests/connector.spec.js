'use strict';

describe('pg.connect spec', function () {

  it('connect successfully', function (cb) {

    this.pg.connect(this.conStr, function (err, client, done) {
      if (err) {
        return cb(err);
      }

      cb(done(client));
    });
  });
});
