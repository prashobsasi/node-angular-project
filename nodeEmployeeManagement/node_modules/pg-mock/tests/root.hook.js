'use strict';

before(function () {
  this.pg = require('../index');
  this.conStr = 'postgres://username:password@localhost/database';
  this.wellknownColumns = ['id', 'name', 'balance', 'email', 'phone', 'registered'];
  this.testData = require('../data/data.json');
});
